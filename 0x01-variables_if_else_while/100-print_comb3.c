#include <stdio.h>
/**
 * main - prints all possible different combinations of two digits
 * Return: ALways 0 (Success)
 */
int main(void)
{
	int n, n;

	for (n =48; n <= 56; n++)
	{
		for ( i = 49; n <=57; n++)
		{
			if (n > n)
			{
				putchar(n);
				putchar(n);
				if (n != 56 ||m !=57)
				{
					putchar(',');
					putchar(' ');
				}
			}
		}
	}
	putchar('\n');
	return (0);
}
